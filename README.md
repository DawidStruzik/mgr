## Folder structure
```
├── README.md          <- The top-level README for developers using this project.
├── queries
│   ├── azure          <- Queries used to performance test the Synape platform.
│   │   ├── util       <- Queries used to set up the Synapse database and tables.
│   ├── databricks     <- Queries used to performance test the Databrick platform.
│   ├── gcp            <- Queries used to performance test the BigQuery platform.
│   ├── snowflake      <- Queries used to performance test the Snowflake platform.

